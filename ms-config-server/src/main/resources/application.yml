server:
  port: 7070


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jhackns/ms-proyect.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
          #username:
          #password:

  security:
    user:
      name: root
      password: 123456